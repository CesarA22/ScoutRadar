{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile.prod"
  },
  "deploy": {
    "startCommand": "sh -c \"npx vite preview --host 0.0.0.0 --port ${PORT:-8080}\"",
    "restartPolicyType": "ON_FAILURE"
  }
}
