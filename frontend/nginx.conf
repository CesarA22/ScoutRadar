server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # Static SPA only. API calls use VITE_API_URL (see src/api/client.ts).
    location / {
        try_files $uri $uri/ /index.html;
    }
}
