# Local dev: leave empty — Vite proxies /api and /health to the backend.
VITE_API_URL=

# Railway: public URL of the BACKEND service (no trailing slash).
# Example: https://scoutradar-backend-production.up.railway.app
# Set this variable on the Railway *frontend* service before deploying.
