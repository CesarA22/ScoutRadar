# Local dev: leave empty — Vite proxies /api to backend.
VITE_API_URL=

# Railway frontend (runtime):
# BACKEND_URL=https://your-backend.up.railway.app
# Start Command: /docker-entrypoint.sh

# Demo videos: committed in public/videos/*.mp4 (~58MB), served as /videos/ from nginx.
# No S3 or presigned URLs needed on the landing page.
