{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "sh -c \"alembic upgrade head && python -m app.scripts.seed_user && uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}\"",
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE"
  }
}
